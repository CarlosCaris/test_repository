# Pasos a seguir para subir codigo a GIT
# Agregar los nuevos cambios no trakeados al STAGING
# $ git add .
# Hacer los cambios COMMIT
# $ git commit -am "mensaje commit"
# Sincronizando los cambios con GIT
# $ git push -u origin main
